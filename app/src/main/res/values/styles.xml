<resources>
    <!-- Existing styles -->
    
    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner.DropDown">
        <item name="android:popupBackground">@drawable/spinner_list_background</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:dropDownHorizontalOffset">60dip</item>
    </style>
</resources> 